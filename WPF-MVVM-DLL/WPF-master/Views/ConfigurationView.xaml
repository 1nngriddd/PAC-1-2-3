<UserControl x:Class="WPF_MVVM_SPA_Template.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{DynamicResource MainBackground}">

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MinWidth="300">

            <TextBlock Text="Configuració" 
                       FontSize="32" 
                       FontWeight="Bold"
                       Margin="0,0,0,30" 
                       HorizontalAlignment="Center"/>         

            <TextBlock Text="Tria el tema de l'aplicació:" 
                       FontSize="16" 
                       Margin="0,0,0,10"
                       HorizontalAlignment="Left"/>

            <ComboBox SelectedIndex="{Binding SelectedThemeIndex}" 
                      Margin="0,0,0,25" 
                      Padding="10,8">
                <ComboBoxItem Content="Tema Clar (Light Theme)"/>
                <ComboBoxItem Content="Tema Fosc (Dark Theme)"/>
            </ComboBox>

            <Button Content="Aplicar Tema" 
                    Command="{Binding ApplyThemeCommand}"
                    Style="{StaticResource ModernPrimaryButtonStyle}" 
                    HorizontalAlignment="Center" 
                    Width="180"
                    Height="45"/>
        </StackPanel>
    </Grid>
</UserControl>
